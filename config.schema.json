{
  "pluginAlias": "WiserHomebridgePlugin",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "secret": {
        "title": "Secret",
        "type": "string",
        "required": true
      },
      "address": {
        "title": "Hub IP Address",
        "type": "string",
        "description": "Local IP address of your Wiser Hub (if set as static on your DHCP)"
      },
      "hideAwayButton": {
        "title": "Hide 'Away' button",
        "type": "boolean",
        "default": false,
        "description": "Hides switch that enables 'Away' mode for your Wiser system"
      }
    }
  }
}